receivers:
  mongodb:
    hosts:
      - endpoint: ${MONGO_HOST}:${MONGO_PORT}
    username: ${MONGO_OTEL_USER}
    password: ${MONGO_OTEL_PASSWORD}
    collection_interval: 10s
    tls:
      insecure: true

service:
  pipelines:
    metrics:
      receivers: [mongodb]
